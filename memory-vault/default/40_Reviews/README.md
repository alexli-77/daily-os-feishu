# Reviews

Store durable review summaries here if you want them in the memory repository.

Runtime daily workflow outputs are still appended to `memory.daily_dir` by
default, which is usually ignored by git.

Suggested structure:

- `daily/YYYY-MM-DD.md`
- `weekly/YYYY-WW.md`
